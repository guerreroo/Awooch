<!DOCTYPE HTML>
<html>
  <head>
    <title>Awooch - Баны</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link rel="stylesheet" type="text/css" href="static/awooch.css">
    <link rel="shortcut icon" href="static/favicon.ico">
  </head>
  <body>
    <a href="/"><img src="/static/logo.png" alt="danger/u/" style="width: 395px; height: 130px;"></a>
    <br />
    <div id="maincontainer" style="overflow: hidden; color: white; padding: 10px;">
      <div class="title" style="color: #ED145B;">Showing Global Ban List</div>
      <br>
      <% query(con, "SELECT * FROM bans WHERE date_of_unban > CURRENT_TIMESTAMP()").each do |res| %>
        <div class="comment">IP: <a href="/ip/<%= Sanitize.clean(res["ip"])%>" style="text-align: left; display: inline-block;"><span class="redtext"><%= Sanitize.clean(res["ip"])%></span></a></div>
        <div class="comment">Забанен на доске: <span class="redtext">/<%= Sanitize.clean(res["board"])%>/</span>, for reason: <span class="redtext"><%= Sanitize.clean(res["reason"])%></span></div>
        <div class="comment">Дата разбана: <span class="redtext"><%= Sanitize.clean(res["date_of_unban"])%></span></div>
        <hr />
      <% end %>
    </div>
    <footer class="comment" style="font-size: xx-small; text-align: center;">
      <%= config["footer"] %>, <a href="http://github.com/" style="font-size: x-small;">Ауч! <%= settings.awoo_version %></a>
    </footer>
  </body>
</html>
